RulesDetailsTO:
  required:
    - rules
    - defaultRules
  properties:
    rules:
      $ref: "#/RulesMapping"
    defaultRules:
      type: array
      items:
        $ref: "../types/rules.yaml#/ruleKey"
      $ref: "../types/rules.yaml#/defaultRuleKeys"

RulesMapping:
  type: object
  additionalProperties:
    $ref: "../types/rules.yaml#/ruleKey"
  $ref: "../types/rules.yaml#/rulesMapping"