post:
  tags: [ "Events" ]
  summary: "Create Milestone Event"
  description: "Creates a new Milestone Event"
  parameters:
    - in: path
      $ref: "../types/route_params.yaml#/runId"
  requestBody:
    description: "Parameters for the newly created Milestone Event"
    required: true
    content:
      application/json:
        schema:
          $ref: "../entities/events/milestones.yaml#/CreateMilestoneEventTO"
  responses:
    "201":
      description: "Created Milestone Event"
      content:
        application/json:
          schema:
            $ref: "../entities/events/milestones.yaml#/MilestoneEventTO"
    "400":
      description: "Validation error in the creation parameters"
    "401":
      description: "Tried to access a Nuzlocke Run of another user without permission"
    "404":
      description: "The Nuzlocke Run does not exist"