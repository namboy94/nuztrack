get:
  tags: [ "Information" ]
  summary: "Get Games"
  description: "Retrieves a mapping of game keys to game titles"
  responses:
    "200":
      description: "Success"
      content:
        application/json:
          schema:
            $ref: "../entities/games.yaml#/GamesMapping"