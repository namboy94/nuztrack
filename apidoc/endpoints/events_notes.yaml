post:
  tags: [ "Events" ]
  summary: "Create Note Event"
  description: "Creates a new Note Event"
  parameters:
    - in: path
      $ref: "../types/route_params.yaml#/runId"
  requestBody:
    description: "Parameters for the newly created Note Event"
    required: true
    content:
      application/json:
        schema:
          $ref: "../entities/events/notes.yaml#/CreateNoteEventTO"
  responses:
    "201":
      description: "Created Note Event"
      content:
        application/json:
          schema:
            $ref: "../entities/events/notes.yaml#/NoteEventTO"
    "400":
      description: "Validation error in the creation parameters"
    "401":
      description: "Tried to access a Nuzlocke Run of another user without permission"
    "404":
      description: "The Nuzlocke Run does not exist"